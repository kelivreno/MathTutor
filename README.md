# Math Tutor
Used for-loops to generate questions asked by the user to generate random mulitiplication table
questions using the (`random.randint()`) function from the random library, and store both integers in a list
and call them again in a for-loop at the end with specific indices
